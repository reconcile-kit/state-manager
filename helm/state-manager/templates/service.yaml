apiVersion: v1
kind: Service
metadata:
  name: {{ include "state-manager.fullname" . }}
  labels:
    {{- include "state-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "state-manager.selectorLabels" . | nindent 4 }}
